version: v2
name: testserver
services:
- name: web
  run: "python server.py"
  type: web
  instances: 1
  cpuCores: 0.1
  ramMegabytes: 400
  terminationGracePeriodSeconds: 30
  port: 3000
build:
  context: ./
  method: docker
  dockerfile: Dockerfile
